import { faker } from '@faker-js/faker';
import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { useState } from 'react';

import {
  AppBar,
  ChatList,
  Integrations,
  Logo,
  Notifications,
  PassengerInfo,
  Theme,
  ThemeProvider,
  TopDrivers,
  TripInfo,
  mq,
} from '@terenceodonoghue/react-components/velocity';
import {
  GitHub,
  InVision,
  Medium,
  Slack,
  Twitter,
} from '@terenceodonoghue/react-icons/brands';
import {
  Check,
  Dynamic,
  Energy,
  Marker,
} from '@terenceodonoghue/react-icons/velocity';

<Meta title="Components/Velocity/Organisms" />

<ThemeProvider
  styles={({ color }) => [
    mq({
      '[data-has-bg] .docs-story': {
        backgroundColor: color.neutral[100],
      },
      '.chat-list': {
        margin: '0 auto',
        maxWidth: [undefined, '378px'],
      },
      '.passenger-info': {
        margin: '0 auto',
        maxWidth: 'fit-content',
      },
      '.top-drivers': {
        margin: '0 auto',
        maxWidth: 'fit-content',
      },
      '.trip-info': {
        margin: '0 auto',
        maxWidth: 'fit-content',
      },
    }),
  ]}
>

<Logo style={{ marginBottom: '1.5rem' }} />

# Organisms

Organisms are relatively complex UI components composed of groups of molecules and/or atoms and/or other organisms. These organisms form distinct sections of an interface.

## App Bar

```javascript
import { AppBar } from '@terenceodonoghue/react-components/velocity';
```

<Canvas>
  <Story name="AppBar">
    <AppBar
      avatar={faker.image.avatar()}
      drawerHandler={() => null}
      messageHandler={() => null}
      notificationHandler={() => null}
      profileHandler={() => null}
      hasMessage
      hasNotification
    />
  </Story>
</Canvas>

## Chat List

```javascript
import { ChatList } from '@terenceodonoghue/react-components/velocity';
```

<Canvas data-has-bg>
  <Story
    name="ChatList"
    decorators={[
      (Story) => (
        <div className="chat-list">
          <Story />
        </div>
      ),
    ]}
  >
    <ChatList
      chats={[
        {
          avatar: faker.image.avatar(),
          name: faker.name.fullName(),
          time: '2m',
          message: faker.lorem.sentence(),
          online: true,
        },
        {
          avatar: faker.image.avatar(),
          name: faker.name.fullName(),
          time: '51m',
          message: faker.lorem.sentence(),
          online: true,
          selected: true,
        },
        {
          avatar: faker.image.avatar(),
          name: faker.name.fullName(),
          time: '2h',
          message: faker.lorem.sentence(),
        },
      ]}
    />
  </Story>
</Canvas>

## Top Drivers

```javascript
import { TopDrivers } from '@terenceodonoghue/react-components/velocity';
```

<Canvas data-has-bg>
  <Story
    name="TopDrivers"
    decorators={[
      (Story) => (
        <div className="top-drivers">
          <Story />
        </div>
      ),
    ]}
  >
    <TopDrivers
      drivers={[
        {
          name: 'Bebop',
          totalEarnings: '$6432',
          totalDistance: '1,232 miles',
        },
        {
          name: 'Gran Tesoro',
          totalEarnings: '$5342',
          totalDistance: '945 miles',
        },
        {
          name: 'Belafonte',
          totalEarnings: '$5133',
          totalDistance: '834 miles',
        },
        {
          name: 'Chester',
          totalEarnings: '$4755',
          totalDistance: '812 miles',
        },
        {
          name: 'Expedia',
          totalEarnings: '$4140',
          totalDistance: '724 miles',
        },
        {
          name: 'Aeolus',
          totalEarnings: '$3323',
          totalDistance: '466 miles',
        },
      ].map((driver) => ({
        ...driver,
        avatar: faker.image.avatar(),
        vehicle: faker.vehicle.vehicle(),
      }))}
    />
  </Story>
</Canvas>

## Passenger Info

```javascript
import { PassengerInfo } from '@terenceodonoghue/react-components/velocity';
```

<Canvas data-has-bg>
  <Story
    name="PassengerInfo"
    decorators={[
      (Story) => (
        <div className="passenger-info">
          <Story />
        </div>
      ),
    ]}
  >
    <PassengerInfo
      avatar={faker.image.avatar()}
      city={faker.address.cityName()}
      email={faker.internet.exampleEmail()}
      name={faker.name.fullName()}
      paymentMethod="visa"
      phoneNumber={faker.phone.number('+144–####–####')}
      state={faker.address.stateAbbr()}
    />
  </Story>
</Canvas>

## Trip Info

```javascript
import { TripInfo } from '@terenceodonoghue/react-components/velocity';
```

<Canvas data-has-bg>
  <Story
    name="TripInfo"
    decorators={[
      (Story) => (
        <div className="trip-info">
          <Story />
        </div>
      ),
    ]}
  >
    <TripInfo
      origin={faker.address.city()}
      originAddress={faker.address.streetAddress()}
      destination={faker.address.city()}
      destinationAddress={faker.address.streetAddress()}
      distance="12.3km"
      time="42 min"
      price="$34.20"
      energy="12.4 kWh"
    />
  </Story>
</Canvas>

## Integrations

```javascript
import { Integrations } from '@terenceodonoghue/react-components/velocity';
```

<Canvas data-has-bg>
  <Story name="Integrations">
    <Integrations
      providers={[
        {
          label: 'InVision',
          description: 'Boards and prototypes',
          icon: InVision,
          enabled: true,
        },
        {
          label: 'GitHub',
          description: 'Commits data and history',
          icon: GitHub,
          enabled: true,
        },
        {
          label: 'Slack',
          description: 'Messages and channels',
          icon: Slack,
        },
        {
          label: 'Twitter',
          description: 'Tweets data',
          icon: Twitter,
        },
        {
          label: 'Medium',
          description: 'Followers count',
          icon: Medium,
        },
      ]}
    />
  </Story>
</Canvas>

## Notifications

```javascript
import { Notifications } from '@terenceodonoghue/react-components/velocity';
```

<Canvas data-has-bg>
  <Story name="Notifications">
    <Notifications
      options={[
        {
          label: 'Email Notification',
          description: 'Commits data and history',
        },
        {
          label: 'Push Notification',
          description: 'Commits data and history',
          defaultChecked: true,
        },
        {
          label: 'Monthly Reports',
          description: 'Commits data and history',
        },
        {
          label: 'Quarter Reports',
          description: 'Commits data and history',
        },
      ]}
    />
  </Story>
</Canvas>

## Theme

```javascript
import { Theme } from '@terenceodonoghue/react-components/velocity';
```

<Canvas data-has-bg>
  <Story name="Theme">
    <Theme
      options={[
        {
          label: 'Shelob',
          colors: ['#2e5bff', '#e0e7ff', '#8097b1'],
          defaultChecked: true,
        },
        {
          label: 'Denethor',
          colors: ['#8c54ff', '#00c1d4', '#fad050'],
        },
        {
          label: 'Quickbeam',
          colors: ['#00a4de', '#3b7ed5', '#00a550'],
        },
        {
          label: 'Shadowfax',
          colors: ['#232a64', '#85c800', '#616266'],
        },
        {
          label: 'Grima',
          colors: ['#a728a3', '#f7e0ff', '#7675ac'],
        },
      ]}
    />
  </Story>
</Canvas>

</ThemeProvider>
