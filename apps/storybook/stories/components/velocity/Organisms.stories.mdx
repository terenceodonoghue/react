import { faker } from '@faker-js/faker';
import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { useState } from 'react';

import {
  AppBar,
  Conversations,
  Integrations,
  Logo,
  Notifications,
  PassengerInfo,
  Themes,
  ThemeProvider,
  Drivers,
  TripInfo,
  defaultTheme,
} from '@terenceodonoghue/react-components/velocity';
import {
  GitHub,
  InVision,
  Medium,
  Slack,
  Twitter,
} from '@terenceodonoghue/react-icons/brands';
import {
  Check,
  Dynamic,
  Energy,
  Marker,
} from '@terenceodonoghue/react-icons/velocity';

<Meta
  title="Components/Velocity/Organisms"
  decorators={[
    (Story) => (
      <ThemeProvider>
        <Story />
      </ThemeProvider>
    ),
  ]}
/>

<Logo style={{ marginBottom: '1.5rem' }} />

# Organisms

Organisms are relatively complex UI components composed of groups of molecules and/or atoms and/or other organisms. These organisms form distinct sections of an interface.

## App Bar

```javascript
import { AppBar } from '@terenceodonoghue/react-components/velocity';
```

<Canvas style={{ background: defaultTheme.color.neutral[100] }}>
  <Story name="AppBar">
    <AppBar
      avatar={faker.image.avatar()}
      drawerHandler={() => null}
      messageHandler={() => null}
      notificationHandler={() => null}
      profileHandler={() => null}
      hasMessage
      hasNotification
    />
  </Story>
</Canvas>

## Conversations

```javascript
import { Conversations } from '@terenceodonoghue/react-components/velocity';
```

<Canvas style={{ background: defaultTheme.color.neutral[100] }}>
  <Story name="Conversations">
    <Conversations
      list={[
        {
          avatar: faker.image.avatar(),
          name: faker.name.fullName(),
          time: '2m',
          message: faker.lorem.sentence(),
          unread: true,
        },
        {
          avatar: faker.image.avatar(),
          name: faker.name.fullName(),
          time: '51m',
          message: faker.lorem.sentence(),
          unread: true,
          defaultChecked: true,
        },
        {
          avatar: faker.image.avatar(),
          name: faker.name.fullName(),
          time: '2h',
          message: faker.lorem.sentence(),
        },
      ]}
    />
  </Story>
</Canvas>

## Drivers

```javascript
import { Drivers } from '@terenceodonoghue/react-components/velocity';
```

<Canvas style={{ background: defaultTheme.color.neutral[100] }}>
  <Story name="Drivers">
    <Drivers
      list={[
        {
          avatar: faker.image.avatar(),
          name: 'Bebop',
          vehicle: faker.vehicle.vehicle(),
          distance: '1,232 miles',
          earnings: 6432,
        },
        {
          avatar: faker.image.avatar(),
          name: 'Gran Tesoro',
          vehicle: faker.vehicle.vehicle(),
          distance: '945 miles',
          earnings: 5432,
        },
        {
          avatar: faker.image.avatar(),
          name: 'Belafonte',
          vehicle: faker.vehicle.vehicle(),
          distance: '834 miles',
          earnings: 5133,
        },
        {
          avatar: faker.image.avatar(),
          name: 'Chester',
          vehicle: faker.vehicle.vehicle(),
          distance: '812 miles',
          earnings: 4755,
        },
        {
          avatar: faker.image.avatar(),
          name: 'Expedia',
          vehicle: faker.vehicle.vehicle(),
          distance: '724 miles',
          earnings: 4140,
        },
        {
          avatar: faker.image.avatar(),
          name: 'Aeolus',
          vehicle: faker.vehicle.vehicle(),
          distance: '466 miles',
          earnings: 3323,
        },
      ]}
    />
  </Story>
</Canvas>

## PassengerInfo Info

```javascript
import { PassengerInfo } from '@terenceodonoghue/react-components/velocity';
```

<Canvas style={{ background: defaultTheme.color.neutral[100] }}>
  <Story name="PassengerInfo">
    <PassengerInfo
      avatar={faker.image.avatar()}
      city={faker.address.cityName()}
      email={faker.internet.exampleEmail()}
      interactions={4}
      name={faker.name.fullName()}
      paymentMethod="visa"
      phoneNumber={faker.phone.number('+144–####–####')}
      state={faker.address.stateAbbr()}
    />
  </Story>
</Canvas>

## TripInfo Info

```javascript
import { TripInfo } from '@terenceodonoghue/react-components/velocity';
```

<Canvas style={{ background: defaultTheme.color.neutral[100] }}>
  <Story name="TripInfo">
    <TripInfo
      fromStreet={faker.address.streetAddress()}
      fromCity={faker.address.city()}
      toStreet={faker.address.streetAddress()}
      toCity={faker.address.city()}
      distance={12.3}
      time={42}
      price={34.2}
      energy={12.4}
    />
  </Story>
</Canvas>

## Integrations

```javascript
import { Integrations } from '@terenceodonoghue/react-components/velocity';
```

<Canvas style={{ background: defaultTheme.color.neutral[100] }}>
  <Story name="Integrations">
    <Integrations
      list={[
        {
          label: 'InVision',
          description: 'Boards and prototypes',
          icon: InVision,
          defaultChecked: true,
        },
        {
          label: 'GitHub',
          description: 'Commits data and history',
          icon: GitHub,
          defaultChecked: true,
        },
        {
          label: 'Slack',
          description: 'Messages and channels',
          icon: Slack,
        },
        {
          label: 'Twitter',
          description: 'Tweets data',
          icon: Twitter,
        },
        {
          label: 'Medium',
          description: 'Followers count',
          icon: Medium,
        },
      ]}
    />
  </Story>
</Canvas>

## Notifications

```javascript
import { Notifications } from '@terenceodonoghue/react-components/velocity';
```

<Canvas style={{ background: defaultTheme.color.neutral[100] }}>
  <Story name="Notifications">
    <Notifications
      list={[
        {
          label: 'Email Notification',
          description: 'Commits data and history',
        },
        {
          label: 'Push Notification',
          description: 'Commits data and history',
          defaultChecked: true,
        },
        {
          label: 'Monthly Reports',
          description: 'Commits data and history',
        },
        {
          label: 'Quarter Reports',
          description: 'Commits data and history',
        },
      ]}
    />
  </Story>
</Canvas>

## Themes

```javascript
import { Themes } from '@terenceodonoghue/react-components/velocity';
```

<Canvas style={{ background: defaultTheme.color.neutral[100] }}>
  <Story name="Themes">
    <Themes
      options={[
        {
          label: 'Shelob',
          colors: ['#2e5bff', '#e0e7ff', '#8097b1'],
          defaultChecked: true,
        },
        {
          label: 'Denethor',
          colors: ['#8c54ff', '#00c1d4', '#fad050'],
        },
        {
          label: 'Quickbeam',
          colors: ['#00a4de', '#3b7ed5', '#00a550'],
        },
        {
          label: 'Shadowfax',
          colors: ['#232a64', '#85c800', '#616266'],
        },
        {
          label: 'Grima',
          colors: ['#a728a3', '#f7e0ff', '#7675ac'],
        },
      ]}
    />
  </Story>
</Canvas>
