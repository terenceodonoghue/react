import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { useState } from 'react';
import { useInterval } from '../src';

<Meta title="Web APIs/Window" />

# Window

The [Window interface](https://developer.mozilla.org/en-US/docs/Web/API/Window) is home to a variety of functions, namespaces, objects, and constructors which are not necessarily directly associated with the concept of a user interface window. However, the Window interface is a suitable place to include these items that need to be globally available. Many of these are documented in the [JavaScript Reference](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference) and the [DOM Reference](https://developer.mozilla.org/en-US/docs/Web/API/Document_Object_Model).

## useInterval

Repeatedly calls a function or executes a code snippet, with a fixed time delay between each call.

```javascript
import { useInterval } from '@terenceodonoghue/react-hooks';
```

<Canvas>
  <Story name="useIntervalHook">
    {() => {
      const [active, setActive] = useState(false);
      const [number, setNumber] = useState(0);
      useInterval(() => setNumber((e) => e + 1), 1000, active);
      return (
        <div style={{ display: 'flex', gap: 16 }}>
          <button onClick={() => setActive(!active)}>
            {active ? 'Stop' : 'Start'}
          </button>
          {number}
        </div>
      );
    }}
  </Story>
</Canvas>
