import { faker } from '@faker-js/faker';
import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { useState } from 'react';

import {
  Conversation,
  Customer,
  Driver,
  Integration,
  Logo,
  Metric,
  NavItem,
  KanbanCard,
  PaymentMethod,
  Setting,
  Theme,
  ThemeProvider,
  mq,
} from '@terenceodonoghue/react-components/velocity';
import { InVision, GitHub } from '@terenceodonoghue/react-icons/brands';
import { Dashboard } from '@terenceodonoghue/react-icons/velocity';

<Meta title="Components/Velocity/Molecules" />

<ThemeProvider
  styles={({ color }) => [
    mq({
      '[data-has-bg] .docs-story': {
        backgroundColor: color.neutral[100],
      },
      '.conversation': {
        display: 'grid',
        gridTemplateColumns: ['1fr', 'repeat(3, 1fr)'],
        gap: 12,
      },
      '.customer': {
        display: 'flex',
        flexDirection: 'column',
        gap: 16,
        margin: '0 auto',
        maxWidth: 'fit-content',
      },
      '.driver': {
        display: 'flex',
        flexDirection: 'column',
        gap: 16,
        margin: '0 auto',
        maxWidth: 'fit-content',
      },
      '.integration-card': {
        display: 'grid',
        gridTemplateColumns: ['1fr', 'repeat(2, 1fr)'],
        gap: [16, 48],
        margin: '0 auto',
        maxWidth: 'fit-content',
      },
      '.kanban-card': {
        display: 'grid',
        gridTemplateColumns: [
          'repeat(1, 1fr)',
          'repeat(2, 1fr)',
          'repeat(3, 1fr)',
        ],
        gap: 24,
      },
      '.metric': {
        display: 'grid',
        gridTemplateColumns: [
          'repeat(2, minmax(0, 158px))',
          'repeat(2, minmax(0, 342px))',
          'repeat(2, minmax(0, 264px))',
        ],
        gap: 24,
        justifyContent: 'center',
      },
      '.nav-item': {
        display: 'grid',
        gridTemplateColumns: 'repeat(2, min-content)',
        gap: 16,
        margin: '0 auto',
        maxWidth: 'fit-content',
      },
      '.payment-method': {
        display: 'grid',
        gridTemplateColumns: ['repeat(2, 1fr)', 'repeat(4, 1fr)'],
        gap: [16, 24],
        margin: '0 auto',
        maxWidth: 'fit-content',
      },
      '.setting': {
        display: 'grid',
        gridTemplateColumns: ['repeat(1, 1fr)', 'repeat(2, 1fr)'],
        columnGap: 64,
        rowGap: 20,
        margin: '0 auto',
        maxWidth: 'fit-content',
      },
      '.theme': {
        display: 'grid',
        gridTemplateColumns: [
          'repeat(auto-fit, 140px)',
          'repeat(auto-fit, 120px)',
        ],
        justifyContent: 'center',
        gap: [24, 32],
      },
      '.trip-details': {
        margin: '0 auto',
        maxWidth: 'fit-content',
      },
      '.trip-summary': {
        margin: '0 auto',
        maxWidth: 'fit-content',
      },
    }),
  ]}
>

<Logo style={{ marginBottom: '1.5rem' }} />

# Molecules

Molecules are relatively simple groups of UI elements (atoms) functioning together as a unit.

## Customer

```javascript
import { Customer } from '@terenceodonoghue/react-components/velocity';
```

<Canvas>
  <Story
    name="Customer"
    decorators={[
      (Story) => (
        <div className="customer">
          <Story />
        </div>
      ),
    ]}
  >
    <Customer
      avatar={faker.image.avatar()}
      name={faker.name.fullName()}
      interactions={1}
    />
    <Customer
      avatar={faker.image.avatar()}
      name={faker.name.fullName()}
      interactions={2}
    />
  </Story>
</Canvas>

## Driver

```javascript
import { Driver } from '@terenceodonoghue/react-components/velocity';
```

<Canvas>
  <Story
    name="Driver"
    decorators={[
      (Story) => (
        <div className="driver">
          <Story />
        </div>
      ),
    ]}
  >
    <Driver
      avatar={faker.image.avatar()}
      name="Bebop"
      vehicle={faker.vehicle.vehicle()}
      distance="1,232 miles"
      earnings={6432}
      rank={1}
    />
    <Driver
      avatar={faker.image.avatar()}
      name="Gran Tesoro"
      vehicle={faker.vehicle.vehicle()}
      distance="945 miles"
      earnings={5432}
    />
  </Story>
</Canvas>

## NavItem

<Canvas>
  <Story
    name="NavItem"
    decorators={[
      (Story) => (
        <div className="nav-item">
          <Story />
        </div>
      ),
    ]}
  >
    <NavItem icon="dashboard" label="Overview" compact selected />
    <NavItem icon="dashboard" label="Overview" selected />
    <NavItem icon="dashboard" label="Overview" compact />
    <NavItem icon="dashboard" label="Overview" />
  </Story>
</Canvas>

## Setting

```javascript
import { Setting } from '@terenceodonoghue/react-components/velocity';
```

<Canvas>
  <Story
    name="Setting"
    decorators={[
      (Story) => (
        <div className="setting">
          <Story />
        </div>
      ),
    ]}
  >
    <Setting
      label="Email Notification"
      description="Commits data and history"
    />
    <Setting
      label="Push Notification"
      description="Commits data and history"
      defaultChecked
    />
  </Story>
</Canvas>

## Conversation

```javascript
import { Conversation } from '@terenceodonoghue/react-components/velocity';
```

<Canvas>
  <Story
    name="Conversation"
    decorators={[
      (Story) => (
        <div className="conversation">
          <Story />
        </div>
      ),
    ]}
  >
    <Conversation
      avatar={faker.image.avatar()}
      name={faker.name.fullName()}
      time="2m"
      message={faker.lorem.sentence()}
      unread
    />
    <Conversation
      avatar={faker.image.avatar()}
      defaultChecked
      name={faker.name.fullName()}
      time="51m"
      message={faker.lorem.sentence()}
    />
    <Conversation
      avatar={faker.image.avatar()}
      name={faker.name.fullName()}
      time="40m"
      message={faker.lorem.sentence()}
    />
  </Story>
</Canvas>

## Integration

```javascript
import { Integration } from '@terenceodonoghue/react-components/velocity';
```

<Canvas>
  <Story
    name="Integration"
    decorators={[
      (Story) => (
        <div className="integration-card">
          <Story />
        </div>
      ),
    ]}
  >
    <Integration
      icon={InVision}
      label="InVision"
      description="Boards and prototypes"
      defaultChecked
    />
    <Integration
      icon={GitHub}
      label="GitHub"
      description="Commits data and history"
    />
  </Story>
</Canvas>

## Kanban Card

```javascript
import { KanbanCard } from '@terenceodonoghue/react-components/velocity';
```

<Canvas data-has-bg>
  <Story
    name="KanbanCard"
    decorators={[
      (Story) => (
        <div className="kanban-card">
          <Story />
        </div>
      ),
    ]}
  >
    <KanbanCard
      color="#2E5BFF"
      name="Spire"
      description="Tire replacement"
      cost="$233"
      date="June 12"
    />
    <KanbanCard
      color="#8C54FF"
      name="Expedition"
      description="Tire replacement"
      cost="$766"
      date="June 3"
    />
    <KanbanCard
      color="#00C1D4"
      name="Scorpion"
      description="Monthly service"
      cost="$430"
      date="June 4"
    />
    <KanbanCard
      color="#F7C137"
      name="Empire"
      description="Engine check-up"
      cost="$542"
      date="June 24"
    />
  </Story>
</Canvas>

## Metric

```javascript
import { Metric } from '@terenceodonoghue/react-components/velocity';
```

<Canvas data-has-bg>
  <Story
    name="Metric"
    decorators={[
      (Story) => (
        <div className="metric">
          <Story />
        </div>
      ),
    ]}
  >
    <Metric
      color="#33AC2E"
      icon="check"
      label="Vehicles on track"
      value="1,428"
    />
    <Metric
      color="#00C1D4"
      icon="marker"
      label="Distance driven"
      value="158.3 mi"
    />
    <Metric
      color="#8C54FF"
      icon="energy"
      label="Energy consumed"
      value="87.4 kWh"
    />
    <Metric
      color="#F7C137"
      icon="dynamic"
      label="Total drive time"
      value="24.2 h"
    />
  </Story>
</Canvas>

## Payment Method

```javascript
import { PaymentMethod } from '@terenceodonoghue/react-components/velocity';
```

<Canvas>
  <Story
    name="PaymentMethod"
    decorators={[
      (Story) => (
        <div className="payment-method">
          <Story />
        </div>
      ),
    ]}
  >
    <PaymentMethod variant="applepay" />
    <PaymentMethod variant="applepay" selected />
    <PaymentMethod variant="mastercard" />
    <PaymentMethod variant="mastercard" selected />
    <PaymentMethod variant="paypal" />
    <PaymentMethod variant="paypal" selected />
    <PaymentMethod variant="visa" />
    <PaymentMethod variant="visa" selected />
  </Story>
</Canvas>

## Theme

```javascript
import { Theme } from '@terenceodonoghue/react-components/velocity';
```

<Canvas>
  <Story
    name="Theme"
    decorators={[
      (Story) => (
        <div className="theme">
          <Story />
        </div>
      ),
    ]}
  >
    <Theme
      label="Shelob"
      colors={['#2e5bff', '#e0e7ff', '#8097b1']}
      defaultChecked
    />
    <Theme label="Denethor" colors={['#8c54ff', '#00c1d4', '#fad050']} />
  </Story>
</Canvas>

</ThemeProvider>
