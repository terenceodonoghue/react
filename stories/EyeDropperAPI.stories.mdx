import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { useState } from 'react';
import { useEyeDropper } from '../src';

<Meta title="Web APIs/EyeDropper API" />

# EyeDropper API

The [EyeDropper API](https://developer.mozilla.org/en-US/docs/Web/API/EyeDropper_API) provides a mechanism for creating an eyedropper tool. Using this tool, users can sample colors from their screens, including outside of the browser window.

## useEyeDropper

```javascript
import { useEyeDropper } from '@terenceodonoghue/react-hooks';
```

<Canvas>
  <Story name="useEyeDropperHook">
    {() => {
      const [colour, getColour] = useEyeDropper();
      return (
        <div style={{ display: 'flex', gap: 16 }}>
          <button onClick={getColour}>Get a colour!</button>
          <div
            style={{
              backgroundColor: colour,
              width: 40,
            }}
          />
        </div>
      );
    }}
  </Story>
</Canvas>
