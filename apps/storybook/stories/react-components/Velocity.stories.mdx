import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { Button } from '@terenceodonoghue/react-components/velocity';

<Meta
  title="Components/Velocity"
  decorators={[
    (Story) => (
      <div style={{ display: 'flex', gap: 32, justifyContent: 'center' }}>
        <Story />
      </div>
    ),
  ]}
/>

# Velocity

A UI kit for an imaginary self-driving car company, provided by [InVision](https://www.invisionapp.com/inside-design/design-resources/design-system-dashboard-ui-kit/). Check out the live demo [here](https://velocity.terenceodonoghue.dev/)!

## Button

```javascript
import { Button } from '@terenceodonoghue/react-components/velocity';
```

The `Button` comes with three variants: contained (default), outlined, and text.

<Canvas>
  <Story name="Button">
    <Button variant="contained">Contained</Button>
    <Button variant="outlined">Outlined</Button>
    <Button variant="text">Text</Button>
  </Story>
</Canvas>
