import { faker } from '@faker-js/faker';
import {
  Canvas,
  Meta,
  Story,
  IconGallery,
  IconItem,
} from '@storybook/addon-docs';
import { useState } from 'react';

import { Flex } from '@terenceodonoghue/react-components/core';
import {
  Avatar,
  Backdrop,
  Button,
  Card,
  Logo,
  Radio,
  Slider,
  Switch,
  TextField,
  ThemeProvider,
} from '@terenceodonoghue/react-components/velocity';
import '@terenceodonoghue/react-components/velocity.css';
import * as Velocity from '@terenceodonoghue/react-icons/velocity';
import {
  Analytics,
  Check,
  Dashboard,
  Dynamic,
  Energy,
  Mail,
  Map,
  Marker,
  Service,
  Settings,
  Vehicles,
} from '@terenceodonoghue/react-icons/velocity';

<Meta
  title="Components/Velocity/Atoms"
  decorators={[
    (Story) => (
      <ThemeProvider>
        <Flex gap={32} alignItems="center" justifyContent="center" wrap="wrap">
          <Story />
        </Flex>
      </ThemeProvider>
    ),
  ]}
/>

<Logo style={{ marginBottom: '1.5rem' }} />

# Atoms

Atoms are the foundational building blocks of a user interface. They include basic HTML elements such as buttons, inputs and icons.

## Button

Buttons allow users to trigger an action or event with a single click.

```javascript
import { Button } from '@terenceodonoghue/react-components/velocity';
```

The `variant` prop defines one of three styles: contained (default), outlined, or text. Standard [button](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/button) attributes are also supported, e.g. `disabled`.

<Canvas>
  <Story name="Button">
    <Button variant="contained">Contained</Button>
    <Button variant="outlined">Outlined</Button>
    <Button variant="text">Text</Button>
    <Button variant="contained" disabled>
      Contained
    </Button>
    <Button variant="outlined" disabled>
      Outlined
    </Button>
    <Button variant="text" disabled>
      Text
    </Button>
  </Story>
</Canvas>

## Radio

Radio buttons allow the user to select one option from a set.

```javascript
import { Radio } from '@terenceodonoghue/react-components/velocity';
```

Text can be placed to the right of the input using the `label` prop, alongside any standard [input](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input) attributes, e.g. `name`, `defaultChecked` or `disabled`.

<Canvas>
  <Story name="Radio">
    <Radio label="Option A" name="radio-group-1" defaultChecked />
    <Radio label="Option B" name="radio-group-1" />
    <Radio label="Option C" name="radio-group-2" defaultChecked disabled />
    <Radio label="Option D" name="radio-group-2" disabled />
  </Story>
</Canvas>

## Slider

Sliders allow users to make selections from a range of values.

```javascript
import { Slider } from '@terenceodonoghue/react-components/velocity';
```

Standard [input](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input) attributes are supported, e.g. `min`, `max`, `step` or `disabled`.

<Canvas>
  <Story name="Slider">
    <Slider />
    <Slider min={0} max={100} step={25} />
    <Slider disabled />
  </Story>
</Canvas>

## Switch

Switches toggle the state of a single setting on or off.

```javascript
import { Switch } from '@terenceodonoghue/react-components/velocity';
```

Standard [input](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input) attributes are supported, e.g. `defaultChecked` or `disabled`.

<Canvas>
  <Story name="Switch">
    <Switch />
    <Switch defaultChecked />
    <Switch defaultChecked disabled />
    <Switch disabled />
  </Story>
</Canvas>

## TextField

Text fields let users enter and edit text.

```javascript
import { TextField } from '@terenceodonoghue/react-components/velocity';
```

Text can be placed above the input using the `label` prop, alongside any standard [input](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input) attributes, e.g. `placeholder`, `type` or `disabled`.

<Canvas>
  <Story name="TextField">
    <TextField label="Label" />
    <TextField label="Email Address" placeholder="samplemail@mail.com" />
    <TextField label="Password" type="password" />
    <TextField label="Disabled" disabled />
  </Story>
</Canvas>

## Avatar

An avatar component represents an object or entity, typically a user's profile picture.

```javascript
import { Avatar } from '@terenceodonoghue/react-components/velocity';
```

The `variant` prop defines one of two styles: rounded (default) or square. Standard [img](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/img) attributes are also supported, e.g. `alt`.

<Canvas>
  <Story name="Avatar">
    <Avatar alt="hello" src={faker.image.avatar()} />
    <Avatar src={faker.image.avatar()} variant="square" />
  </Story>
</Canvas>

Height and width can be customised by providing a number to the `size` prop.

<Canvas>
  <Story name="AvatarSize">
    <Avatar size={32} src={faker.image.avatar()} variant="rounded" />
    <Avatar size={48} src={faker.image.avatar()} variant="rounded" />
    <Avatar size={64} src={faker.image.avatar()} variant="rounded" />
    <Avatar size={80} src={faker.image.avatar()} variant="rounded" />
  </Story>
</Canvas>

## Card

Cards contain content and actions about a single subject.

```javascript
import { Card } from '@terenceodonoghue/react-components/velocity';
```

An optional `heading` prop renders text above the content block.

<Canvas>
  <Story name="Card">
    <Card heading="Heading">Content</Card>
    <Card>Content</Card>
  </Story>
</Canvas>

## Icons

Icons can be used to improve visual interest and draw attention to an element.

```javascript
import /* { IconName } */  from '@terenceodonoghue/react-icons/velocity';
```

The `fill` prop sets the icon colour, or defaults to `currentColor` if left blank. Other properties such as the icon's size can be defined through React's styling API, using either [className](https://reactjs.org/docs/faq-styling.html) (recommended) or [style](https://reactjs.org/docs/dom-elements.html#style).

<Canvas>
  <Story name="Icons">
    <Dashboard style={{ height: 24, width: 24 }} />
    <Analytics fill="#2E5BFF" style={{ height: 24, width: 24 }} />
    <Vehicles fill="#33AC2E" style={{ height: 24, width: 24 }} />
    <Service fill="#8C54FF" style={{ height: 24, width: 24 }} />
    <Map fill="#D63649" style={{ height: 24, width: 24 }} />
    <Mail fill="#00C1D4" style={{ height: 24, width: 24 }} />
    <Settings fill="#F7C137" style={{ height: 24, width: 24 }} />
  </Story>
</Canvas>

Refer to [the icon gallery](?path=/docs/icons-velocity--page) to see the full set of icons.

## Backdrop

A backdrop can be used with icons for added emphasis.

```javascript
import { Backdrop } from '@terenceodonoghue/react-components/velocity';
```

The `color` prop sets the backdrop's colour with a default opacity of `0.15`. This can be adjusted with the `opacity` prop.

<Canvas>
  <Story name="Backdrop">
    <Backdrop color="#33AC2E">
      <Check fill="#8C54FF" style={{ height: 20, width: 20 }} />
    </Backdrop>
    <Backdrop color="#00C1D4">
      <Marker fill="#2E5BFF" style={{ height: 20, width: 20 }} />
    </Backdrop>
    <Backdrop color="#8C54FF">
      <Energy fill="#2E5BFF" style={{ height: 20, width: 20 }} />
    </Backdrop>
    <Backdrop color="#F7C137">
      <Dynamic fill="#2E5BFF" style={{ height: 20, width: 20 }} />
    </Backdrop>
  </Story>
</Canvas>
